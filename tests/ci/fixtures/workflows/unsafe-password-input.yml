name: Unsafe Password Input
on:
  workflow_dispatch:
    inputs:
      admin_password:
        description: 'Admin password to set'
        required: true
        type: string
      secret_token:
        description: 'Secret token'
        required: true
        type: string
permissions:
  contents: read
jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Setting password"
