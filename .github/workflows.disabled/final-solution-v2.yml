# SECURITY TOMBSTONE
#
# Disabled legacy workflow removed from the current tree because it contained
# plaintext credential material or credential-reset behavior.
#
# Do not reactivate. Rebuild the operation using protected GitHub Secrets,
# fail-closed confirmation gates, and independent review.
# Historical content remains in Git history for audit purposes.
