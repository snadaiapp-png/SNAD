# SECURITY TOMBSTONE
#
# This disabled legacy workflow previously contained hardcoded credential material
# or credential-reset behavior. Its executable body was intentionally removed
# from the current repository tree.
#
# Do not restore or reactivate this workflow. Rebuild any required operation from
# the current governed production workflows using protected GitHub Secrets,
# fail-closed confirmation gates, and independent review.
#
# Historical content remains available through Git history for audit purposes.
