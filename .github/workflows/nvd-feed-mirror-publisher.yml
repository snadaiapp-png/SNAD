# ============================================================
# SANAD — NVD Feed Mirror Publisher (LEGACY — DEPRECATED)
# ============================================================
# EXEC-PROMPT-010R12L: This workflow is DEPRECATED.
# The official publisher is now:
#   .github/workflows/nvd-bulk-feed-mirror-publisher.yml
# This file is retained for diagnostic purposes only.
# Do NOT schedule or use for acceptance.
# ============================================================

name: NVD Feed Mirror Publisher (DEPRECATED)

on:
  workflow_dispatch:
  # R12L: schedule removed — replaced by bulk feed publisher

permissions:
  contents: write
  actions: read

concurrency:
  group: snad-nvd-feed-legacy-diagnostic
  cancel-in-progress: false

env:
  PYTHONUNBUFFERED: "1"

jobs:
  publish-feed:
    name: Publish NVD Feed Mirror (Legacy)
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Deprecation notice
        run: |
          echo "::notice::This workflow is DEPRECATED."
          echo "Use NVD Bulk Feed Mirror Publisher instead."
          echo "This workflow is retained for diagnostic purposes only."
          exit 1
