name: Runner Canary

on:
  push:
    branches:
      - infra/02c-remote-ci-validation

permissions:
  contents: read

jobs:
  runner-canary:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner started
        run: |
          echo "RUNNER_CANARY_STARTED"
          uname -a
          echo "RUNNER_CANARY_PASS"
