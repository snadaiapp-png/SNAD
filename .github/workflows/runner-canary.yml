name: Runner Canary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner-canary:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner started
        run: |
          echo "RUNNER_CANARY_STARTED"
          uname -a
          echo "RUNNER_CANARY_PASS"
