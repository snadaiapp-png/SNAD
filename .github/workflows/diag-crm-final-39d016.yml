name: Diagnose Vercel Promotion Rejection

on:
  pull_request:
    paths:
      - '.github/workflows/diag-crm-final-39d016.yml'

permissions:
  contents: read
  actions: read

jobs:
  diagnose:
    name: Read promotion rejection tail
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
      FAILED_JOB_ID: '88644546805'
    steps:
      - name: Print final 36 sanitized lines
        shell: bash
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error --location --max-time 60 \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/jobs/${FAILED_JOB_ID}/logs" \
            > "$RUNNER_TEMP/promotion.log"
          sed -E 's/\x1B\[[0-9;]*[mK]//g' "$RUNNER_TEMP/promotion.log" | tail -n 36
