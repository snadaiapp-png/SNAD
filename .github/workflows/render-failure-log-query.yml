name: Render Failure Log Query

on:
  pull_request:
    branches: [main]
    paths: ['.github/workflows/render-failure-log-query.yml']

permissions:
  contents: read

jobs:
  query:
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      RENDER_CLI_CONFIG_PATH: ${{ github.workspace }}/render-cli.yaml
    steps:
      - name: Configure pinned Render CLI
        shell: bash
        run: |
          set -euo pipefail
          auth=(-H "Authorization: Bearer $RENDER_API_KEY" -H "Accept: application/json")
          curl -fsS "${auth[@]}" "https://api.render.com/v1/services/$RENDER_SERVICE_ID/env-vars?limit=100" > env.json
          jq -r '.[]? | (.envVar // .) | .value // empty | select(length >= 8)' env.json | while IFS= read -r value; do echo "::add-mask::$value"; done
          curl -fsS "${auth[@]}" "https://api.render.com/v1/services/$RENDER_SERVICE_ID" > service.json
          owner=$(jq -r '.ownerId // .service.ownerId // .owner.id // .service.owner.id // empty' service.json)
          test -n "$owner"
          echo "::add-mask::$owner"
          curl -fsSL "https://github.com/render-oss/cli/releases/download/v2.21.0/cli_2.21.0_linux_amd64.zip" -o cli.zip
          unzip -q cli.zip -d cli
          binary=$(find cli -type f -name 'cli_v*' | head -n1)
          chmod +x "$binary"
          echo "RENDER_CLI=$PWD/$binary" >> "$GITHUB_ENV"
          "$binary" workspace set "$owner" --output text

      - name: Print sanitized latest failure logs
        shell: bash
        run: |
          set -euo pipefail
          "$RENDER_CLI" logs --resources "$RENDER_SERVICE_ID" \
            --start 2026-07-04T00:02:00Z --end 2026-07-04T00:07:00Z \
            --type app --limit 1000 --direction forward --output json > logs.jsonl
          python3 - <<'PY'
          import json,re
          from pathlib import Path
          match=re.compile(r'(?i)(error|exception|caused by|failed|fatal|killed|outofmemory|oom|cannot|refused|timeout|flyway|database|postgres|startup|terminated|exit|port|bean|configuration)')
          selected=[]
          for raw in Path('logs.jsonl').read_text(errors='replace').splitlines():
              try:
                  obj=json.loads(raw); msg=str(obj.get('message','')); ts=str(obj.get('timestamp','')); level=str(obj.get('level',''))
              except Exception:
                  msg,ts,level=raw,'',''
              msg=re.sub(r'(?i)(jdbc:postgresql|postgresql|https?)://[^\s"\']+',r'\1://[REDACTED]',msg)
              msg=re.sub(r'(?i)(password|secret|token|api[_-]?key)\s*[=:]\s*\S+',r'\1=[REDACTED]',msg)
              if match.search(msg) or match.search(level): selected.append((ts,level,msg))
          print('MATCHED_LOGS='+str(len(selected)))
          for row in selected[-300:]: print(*row)
          PY
