# Authorized Marker Contract

Only the immutable squash-merge commit message marker `PRODUCTION-RELEASE-AUTHORIZED` authorizes automatic dispatch. Repository files by themselves do not authorize production deployment.
