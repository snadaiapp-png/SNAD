# Self-Review & Coverage Matrix — Corrected Plans vs Design Spec Revision C (§1–§34)

**Reviewed artifacts:** spec + master plan + 7 wave subplans + this checklist as corrected on branch `docs/unified-control-plane-plan-corrections-r2-recovered` (Rev C, REGENERATED FROM SCRATCH after the R2 correction pass — Rev B statuses were NOT carried over mechanically; every row was re-evaluated against the Rev C documents and fresh repository evidence), against `docs/superpowers/specs/2026-09-23-unified-authorization-partner-billing-design.md` Revision C. Provenance chain: Rev A merged @ `8d0d49c7bdd3ad3a886a23cffc1e735e61998712` (PR #1140); Rev B recovered byte-exact from the GitHub recovery branch (`REVISION_B_DOCUMENT_RECOVERY=10/10_EXACT`, Rev B base commit `cb5ef42c7ece6bca5aa401356d4b9ae52219720c`; original provenance SHA `1cd9210940e19898baeedfc469152cc3b161b0de` is PROVENANCE-ONLY and was not restored); Rev C written on top.
**Verdict rule (Rev C):** statuses are limited to `COVERED`, `PARTIAL`, `NOT_APPLICABLE_WITH_REASON`, `BLOCKED`. Nothing is pre-marked COVERED; each row names the plan, task, and test that will prove it. BLOCKED rows are listed EXPLICITLY (this review does NOT claim "BLOCKED rows: none" — two out-of-scope dependency blockers remain, see §A2).

## A. Spec section coverage (§ → requirement → plan → task → test → status)

| SPEC SECTION | REQUIREMENT | PLAN | TASK | TEST | STATUS |
|---|---|---|---|---|---|
| §1 | Purpose / scope statement | Master §0 | — | — | NOT_APPLICABLE_WITH_REASON=scope narrative; no executable requirement |
| §2 | Extend existing foundations; backward compat | Master §0 table | all waves | W1 Task 8 `EvaluateFacadeCompatibilityTest` | COVERED |
| §3 | Hierarchical multi-principal platform + boundaries | W2 | Task 1, Task 12 | `PartnerPrincipalRlsPostgresTest`; portal IT | COVERED |
| §4.1 | Five-stage runtime algorithm (A–E); protected safety NOT a precedence layer; no `PROTECTED_SAFETY` source | W1 | Task 7, Task 8, Task 11 | `UnifiedDecisionPipelineTest`; `DenyDominancePostgresTest`; reflection scan on `DecisionSource` | COVERED |
| §4.2 | Fail-closed list | W1 | Task 7 | `UnifiedDecisionPipelineTest` cases a–c,f,i | COVERED |
| §5 | Protected roles = PLATFORM_OWNER, PLATFORM_ADMIN, AGENT_SUPER_ADMIN, TENANT_ADMIN; AGENT_CUSTOM_ADMIN NOT protected | W1+W2+W7 | W1 Task 3/14, W2 Task 11, W7 Task 3 | `ProtectedRoleRegistrySchemaPostgresTest`; `ProtectedRoleGrantGuardTest`; `ProtectedSeedFourRolesPostgresTest` | COVERED |
| §5.1 | Break-glass rejections + simulation + break-glass evaluated normally (DENY still wins) | W1+W7 | W1 Task 15, W7 Task 5 | `BreakGlassAccessServiceTest`; `BreakGlassInvariantPostgresAcceptanceTest` | COVERED |
| §6 | Registry metadata columns | W1 | Task 4 | `CapabilityMetadataMigrationContractTest` | COVERED |
| §6.1 | ONE canonical Capability Contract Table; no duplicate namespace; full-chain VIEW/MANAGE/FINALIZE + SUBSCRIPTION.MANAGE continuation authority (Rev C) | W2+W3+W4+W5 | W2 Tasks 5/10, W3 Task 6, W4 Tasks 8/9/11, W5 Task 14 | `PartnerCapabilitySeedContractTest`; `CommercialCapabilitySeedContractTest`; `BillingCapabilitySeedContractTest`; `SettlementControllersIT`; `BillingForeignKeyAuditPostgresTest` | COVERED |
| §7 | RBAC + ABAC + ReBAC-lite; explicit relationships; no fuzzy matching | W1 | Task 7, Task 12 | pipeline + `EffectivePermissionProjectionServiceTest` + `HrScopedRelationshipResolver` | COVERED |
| §8 | User/subject vs employee; service accounts as subjects | W1 (existing `users` table = subjects) | documented mapping (master §0) | no schema change required — existing identity tests | NOT_APPLICABLE_WITH_REASON=design reuses the existing users/employees model; no new executable behavior |
| §9 | Overrides + Direct DENY v1 DB invariant + precedence correction | W1 | Task 1, Task 9, Task 11 | `UserPermissionOverrideRlsPostgresTest` (23514 on scoped DENY); `UserPermissionOverrideServiceTest`; `DenyDominancePostgresTest` | COVERED |
| §10 | Dynamic scope family + capability-declared scopes | W1 | Task 1, Task 9 | scope CHECK + `supports_scope` validation matrix | COVERED |
| §11 | Explainable decisions + cache model | W1 | Task 7, Task 13 | `AuthorizationDecision` record; `AuthorizationInvalidationListenerTest` | COVERED |
| §12 | Entitlements vs permissions; recalc vs resync | W1 (+ existing entitlement gate) | Task 10 | `UserOverrideControllerIT` resync 403/200 | COVERED |
| §13 | Delegation over canonical capabilities; scope from authenticated context; SUBSCRIPTION.MANAGE in the delegation vocabulary (Rev C) | W2 | Task 10, Task 12 | `PartnerDelegationGateTest`; `PartnerPortalControllerIT` | COVERED |
| §13.1 | One ACTIVE partner membership per user; immediate session invalidation on create/suspend/downgrade/transfer; no 5 s staleness window on re-auth (Rev C) | W2 | Task 1, Task 6 (G5), Task 7 (four mutation kinds) | `PartnerPrincipalRlsPostgresTest` (23505 race); `PartnerMembershipConcurrencyPostgresTest`; `PartnerSessionInvalidationTest` | COVERED |
| §14 | Business identity fields + 3 mounts | W3 | Task 1, Task 8, Task 11 | `BusinessIdentityRlsPostgresTest`; `BusinessIdentityServiceTest`; controller ITs | COVERED |
| §14.1 | Reusable Commercial & Tax Information screen | W3 | Task 12 | `commercial-api.test.ts` + form component tests | COVERED |
| §14.2 | White-label readiness (branding modeled separately) | W3 | Task 1/9 schema | `brand_profiles` separate table asserted in `LogoUploadServiceTest` | COVERED |
| §14.3 | platform_files: real schema, no `kind`, ownership CHECK, ONE fail-closed principal policy, shared-table regression, `pg_policy` verification, registration-of-already-stored-object scope (Rev C) | W3 | Task 2, Task 3, Task 9 | `PlatformFilesForceRlsPostgresTest` (incl. `pg_policy` assertions); `WorkflowAttachmentExternalFoundationTest`; `LogoUploadServiceTest` | COVERED for the registration scope — the byte-upload capability itself is BLOCKED out-of-scope (see §A2, BLK-1) |
| §15 | Immutable billing identity snapshots; allocate-UUID-first capture order (Rev C) | W3+W4 | W3 Tasks 4/5/10, W4 Tasks 4/14 | `SnapshotImmutabilityPostgresTest`; `SnapshotInvoiceBindingPostgresTest`; issuance chain test | COVERED |
| §16 | DIRECT/RESELLER/COMMISSION/HYBRID | W2+W4 | W2 Task 1, W4 Task 1 | CHECK-constraint assertions in schema tests | COVERED |
| §17 | Partner→tenant invoices MANUAL/AUTOMATIC | W4 | Task 14, Task 17 | `PartnerInvoiceIssuancePostgresTest`; `AutomaticBillingPostgresTest` | COVERED |
| §17.2 | Auto-invoicing carrier = subscription; five-condition scheduler; auditable confirmation; DB-level dual-scheduler idempotency (Rev C) | W4 | Task 2 (`issuance_idempotency_key`), Task 5, Task 6, Task 17 | `LifecycleGateStatusesPostgresTest` (negative column assertion on billing_invoices); `AutomaticBillingPostgresTest` (5 negatives + two-thread single-invoice proof) | COVERED |
| §18.1 | Settlement basis B: net collected; no double subtraction; NEGATIVE RESIDUAL PRESERVED AND CARRIED, never clamped, never a negative customer invoice (Rev C matrix <0/=0/>0) | W5 | Task 4, Task 1 (period CHECK change) | `EligibleNetCalculatorTest` matrix incl. negative-carryover edges | COVERED |
| §18.2 | Fee percent unambiguous (agreement version field) | W4+W5 | W4 Task 1, W5 Task 5 | CHECK bounds; per-item math | COVERED |
| §18.3 | Version immutability + invoice pinning + temporal exclusion | W4 | Task 1, Task 16 | `AgreementTemporalIntegrityPostgresTest` (23P01 + same-start race + open-ended block); `AgreementVersionPinningPostgresTest` | COVERED |
| §18.4 | Settlement lifecycle replay-safe + reconcilable; COMPLETED run always binds its period (Rev C run model) | W5 | Task 5, Task 6, Task 12 | `PartnerSettlementCalculatePostgresTest`; `SettlementReplayReplacePostgresTest`; `SettlementReconciliationPostgresTest`; Task 1 CHECK assertions | COVERED |
| §18.5 | Period state machine: full rebuild; same key no-op; new key replace; finalize immutable at DB level (Rev C triggers); concurrency | W5 | Task 1 (immutability triggers), Task 6, Task 10 | `SettlementReplayReplacePostgresTest`; `SettlementFinalizeConcurrencyPostgresTest`; trigger assertions in `SettlementSchemaFkPostgresTest` | COVERED |
| §18.6 | Late adjustments = QUEUED→APPLIED with atomic claim + idempotency key (Rev C); finalized history untouched | W5 | Task 1 (adjustment model), Task 11 | `LateAdjustmentPostgresTest` (claim concurrency + replay no-op) | COVERED |
| §18.7 | NO persisted period fee; derived weighted display only | W5+W6 | W5 Task 1 (negative column assertion), W6 Task 4 (no column), W5 Task 15 (UI label) | `SettlementSchemaFkPostgresTest`; `ProjectionSchemaExactPostgresTest` | COVERED |
| §19 | Finance authoritative; no second ledger; boundary contract via Finance-owned ports incl. `CollectedCashReadPort` (Rev C); additive correction authority `FinanceCorrectionPort` with exact journal semantics (Rev C); principal-invoice port for SETTLEMENT invoices (Rev C) | W4/W5 | W4 Tasks 14/15/18, W5 Tasks 8/10 | `markInvoicePaid` 409; `R0C13ArchitectureBoundaryTest` + `FinanceReadBoundaryTest`; `FinanceCorrectionAccountingPostgresTest`; principal-invoice assertions in `SettlementApprovalPostgresTest` | COVERED |
| §20 | Logical data model (tables) | W1–W6 migrations | per-wave schema tasks | schema/RLS tests per wave | COVERED |
| §21 | Notification events list + mandatory owner in-product delivery | W6 | Task 2 | `MandatoryOwnerNotificationPostgresTest` | COVERED |
| §21.1 | Denormalized scope + consistency CHECKs + explicit RLS + isolation negatives + ack ownership + audience-source separation + NO COALESCE bypass (Rev C) | W6 | Task 1, Task 3 | `NotificationSchemaRlsPostgresTest`; `NotificationIsolationPostgresTest` (incl. forged-source negative) | COVERED |
| §22 | Three dashboard scopes + governed projections | W6 | Task 4, Task 6, Task 8 | `ProjectionSchemaExactPostgresTest`; `PartnerProjectionPostgresTest`; controller ITs | COVERED |
| §22.4 | Exact typed projection schema; no schematic placeholders; typed apply-log | W6 | Task 4, Task 5 | `ProjectionSchemaExactPostgresTest` (name-by-name); `ProjectionEventTypingPostgresTest` | COVERED |
| §22.5 | GLOBAL = DIRECT + Σ(PARTNERS); fixture ≥2 partners + ≥1 direct tenant | W6 | Task 7 | `DashboardReconciliationPostgresTest` | COVERED |
| §23 | Invoice dashboards (owner + partner views) | W6 | Task 8, Task 9 | `DashboardControllerIT`; FE gating tests | COVERED |
| §24 | Executive/partner/tenant IA | W2/W3/W6 | W2 Task 13, W3 Task 12, W6 Task 9 | FE gating tests + visual-matrix baselines | PARTIAL=component/gating tests only; no cross-browser E2E harness exists in the repo (visual-matrix baselines added in W6 Task 9; execution evidence pending) |
| §25 | Authorization administration UI + user detail panels | W1 | Task 16 | `access-api.test.ts`; `authorization-page-gating.test.tsx` | COVERED |
| §26 | Event-driven invalidation + projections; atomic outbox/audit; canonical projection-backfill semantics (Rev C: deterministic rebuild, idempotent, version-stamped, count-invariant) | W1+W6+W7 | W1 Task 13, W6 Task 6, W7 Task 3 | listener tests; fan-out same-transaction assertions; `ProjectionBackfillPostgresTest` (rebuild-twice checksum + monotonic versions) | COVERED |
| §27 | Audit record fields; DENY/high-risk auditable; no commit-RED-then-later-GREEN in audit TDD (Rev C) | W1 (+ existing PlatformAuditWriter) | Task 6 (migration only), Task 10 (contract test RED→GREEN in one task) | `AccessAdminAuditContractTest` (created in W1 Task 10) | COVERED |
| §28 | API categories fixed (routes are plan details) | W1/W2/W4/W5/W6 controllers | per-wave controller ITs | MockMvc ITs | COVERED |
| §29 | Migration strategy phases 1–9 | Master §1 mapping | per-wave | per-wave | COVERED |
| §29.1 | Progressive cutover G7-A..G7-G; per-stage gates; no big bang; NUMERIC G7-A soak (≥7 days AND ≥50k decisions, divergence 0, p99 ≤10 %); same-SHA contract; committed defaults OFF/legacy; forward-only hardening rollback; per-stage observability implementation+query+evidence (Rev C) | W7 | Tasks 8–14 + Global Constraints 3/4 | stage smoke ITs + per-stage records + soak rows in evidence logs | COVERED |
| §30 | 10 release-blocking security invariants | W7 | Task 4 | `SecurityInvariantsPostgresAcceptanceTest` | COVERED |
| §31.1 | Authorization matrix (subjects × dimensions) | W7 | Task 5 | `AuthorizationMatrixPostgresAcceptanceTest` | COVERED |
| §31.2 | Break-glass test list (7 rejections) | W7 | Task 5 | `BreakGlassInvariantPostgresAcceptanceTest` | COVERED |
| §31.3 | Billing/settlement mandatory tests incl. the Rev C additions (issuance idempotency, boundary test, journal-correction semantics, negative-carryover matrix, single-allocation concurrency, congruence chain, run-model CHECK, adjustment claim atomicity, principal-invoice port) | W4+W5 | W4 Tasks 2/14–18, W5 Tasks 4–13 | named per task (`FinanceCorrectionAccountingPostgresTest`, `FinanceReadBoundaryTest`, `AutomaticBillingPostgresTest` two-thread proof, `LateAdjustmentPostgresTest` claim concurrency, `SettlementApprovalPostgresTest` congruence chain) | COVERED |
| §31.4 | Dashboard reconciliation + direct-API isolation | W6 | Task 7, Task 8 | reconciliation + isolation tests | COVERED |
| §31.5 | PostgreSQL Direct mandatory | all waves | every PG test | local + pg-acceptance profiles with exact commands | COVERED |
| §31.6 | Correction-mandated negative/concurrency tests (19 lines) | W1/W2/W3/W4/W5/W6 | W1 Task 11, W2 Task 7, W3 Tasks 1/2, W4 Task 1, W5 Tasks 6/10/11, W6 Tasks 3/7 | each line mapped to a named test class | COVERED |
| §32 | Release acceptance criteria list incl. the protected release chain with REQUIRED-CHECKS DISCOVERY gate and the authenticated desktop/mobile + RTL/accessibility final gate (Rev C) | W7 | Task 15 | verification runbook §4 steps 7–8 | PARTIAL for the RTL/accessibility/mobile line (depends on visual-matrix baselines added W6 Task 9; execution evidence can only exist after implementation) and BLOCKED for the required-checks discovery line at PLANNING time (see §A2, BLK-2) |
| §33 | Non-goals (no ZATCA/live-payments claims) | W4/W5/W7 | flags unchanged (`R0C13_PROVIDER_MODE`) | documented invariant | NOT_APPLICABLE_WITH_REASON=negative space; compliance certification is out of scope by design |
| §34 | Final architecture decision | Master §0 + waves | — | — | NOT_APPLICABLE_WITH_REASON=architecture statement; no executable requirement |

## A2. Explicit BLOCKED rows (Rev C — no "zero blockers" claim)

| ID | ITEM | STATUS | EVIDENCE (fresh, this correction pass) | RESOLUTION PATH |
|---|---|---|---|---|
| BLK-1 | `REAL_BYTE_STORAGE_ADAPTER` — real byte upload (browser → object storage) for logos/attachments | **BLOCKED — out-of-scope dependency** | No byte-storage adapter exists: `PlatformFileReferenceService` is registration-only (caller-supplied `storage_reference`; javadoc: "no object storage / file service exists in the deployment"); zero S3/MinIO/GCS/Azure deps in `apps/sanad-platform/pom.xml` | Rev C scopes W3 to registration of an already-stored object + `StorageAdapter` port contract (`W3_STORAGE_ADAPTER` resolved BY SCOPE LIMIT); the byte-upload capability is an explicit future dependency requiring a storage-infrastructure decision. Does NOT block W3 execution as scoped; DOES block any claim of end-to-end multipart upload |
| BLK-2 | `RELEASE_REQUIRED_CHECKS_DISCOVERY` — current branch-protection required-check set | **BLOCKED — release-gate dependency** | GitHub API `GET /repos/snadaiapp-png/SNAD/branches/main/protection` ⇒ HTTP 403 (unauthenticated; `gh` CLI absent); the required-check set is NOT in git and CANNOT be read at planning time | Rev C converts this into an explicit release-chain gate: discovery with authenticated token evidence (`required-checks.json`) BEFORE any release claim; candidate job names recorded as candidates only. Does NOT block planning/implementation; DOES block any release claim until discovery evidence exists |

`FINANCE_CREDIT_NOTE_MODEL` is NOT listed as blocked: fresh evidence confirmed no generic partial credit-note primitive exists (`recordRefund` is full-refund-only; adapter throws on partial), and Rev C resolves it by PLANNING the Finance-owned additive correction model with exact accounting semantics as W4 Task 18 (`FinanceCorrectionPort` + `finance_credit_corrections` + balanced journal entries) — the resolution path the review directive prescribes. `W3_STORAGE_ADAPTER` likewise is resolved BY SCOPE LIMIT for the wave, with the residual byte-upload dependency carried honestly as BLK-1.

## B. Quality gates applied to the corrected files (Rev C)

1. **Placeholder/shorthand scan** — the ten corrected files were scanned for: command ellipses, cross-row command references, lettered count placeholders, generic unwritten-count phrases, generic "battery by name only" references, TBD markers, and schematic wildcard DDL. Rev C additionally scanned for the FORBIDDEN GUC bypass pattern `COALESCE(current_setting('app.current_user_id', true)` — the only remaining occurrences are the Rev C prohibition statements themselves (W6 header/Task 1, spec §21.1); no policy or command uses it. Scan executed mechanically; method and result in §D.
2. **Exact path validation (re-run fresh after recovery — no stale evidence reused)** — every pre-existing path cited was re-verified at `8d0d49c7` in THIS session: `platform_files` DDL + ENABLE/policy `platform_files_tenant_isolation` (`V20260911_2` lines 37–39); finance RLS absence for 6 tables (per-table `rg` over `db/migration`); `finance_invoice_number_sequences` ENABLE+FORCE (`V20260820_6`); CRM 51-table list + per-table RLS state (mechanical loop over `CREATE TABLE crm_*` + ENABLE/FORCE probes); `SubscriptionFinancePort` signatures + `recordRefund` full-only semantics (port javadoc + adapter `amountMinor != billing.totalMinor()` throw); `CommerceFinanceAdapter` line-78 INSERT; `FinanceManagementIntegrationService` reads; `BillingReconciliationService` line-93 JOIN; `billing_invoices` V19 DDL (no `issuance_idempotency_key`); capability tables V7/V8/V20260814_2; `ci.yml` expected dict lines 535–539 + `ALLOWED` dict line 1058; web harnesses (`playwright.config.ts`, `playwright.standard.config.ts` invoked by `playwright-ci.yml` line 125, `@axe-core/playwright` 4.12.1 + `@testing-library/react` 16.3.2 + vitest in `apps/web/package.json`, web-ci vitest run + artifact); drift audit `git diff --name-status 8d0d49c7..origin/main` = 20 files (auth-login branding only).
3. **Interface consistency** — table names, event names, scope names, endpoint prefixes, capability codes (`SUBSCRIPTION.MANAGE` continuation authority; `SETTLEMENT.VIEW/MANAGE/FINALIZE` chain), DTO names, port contracts (`FinanceCorrectionPort`, `FinancePrincipalInvoicePort`, `CollectedCashReadPort`) cross-checked within and across waves and against spec §6.1.
4. **Requirement coverage** — table §A; no approved requirement silently absent; architecture-only rows carry explicit `NOT_APPLICABLE_WITH_REASON`; dependency blockers carried explicitly in §A2.
5. **TDD structure** — every task carries Files/Interfaces and six exact steps; Rev C removes the W1 Task 6 deferred-RED pattern (audit contract test RED→GREEN inside W1 Task 10) and resolves the phantom `PartnerIsolationPostgresAcceptanceTest` (created by W7 Task 6 before the CI lockstep consumes it).

## C. Consistency decisions recorded (Rev C)

- `DecisionSource` = `EXPLICIT_DENY, EXPLICIT_ALLOW, ROLE_GRANT, RELATIONSHIP_POLICY, DELEGATED_GRANT, DEFAULT_DENY` — no `PROTECTED_SAFETY` (spec §9).
- Protected registry = exactly 4 codes in W1 migration, W2 grant guard, and W7 seeding; `AGENT_CUSTOM_ADMIN` is a normal hierarchy role (spec §5).
- GUC contract: three GUCs (`app.tenant_id`, `app.partner_id`, `app.current_user_id`) — all sourced from verified server-side authentication; missing ⇒ fail-closed; W2 introduces `app.current_user_id`, W6 consumes it; background jobs have NO implicit bypass (W7 access-path matrix row 7); platform branches require the carrier tenant + `app.partner_id` NULL; the `COALESCE(current_setting('app.current_user_id', true), …)` bypass pattern is FORBIDDEN everywhere (W6 deliveries WITH CHECK uses the two-branch rule).
- Finance boundary: writes only via Finance-owned ports (`SubscriptionFinancePort`, `FinanceCorrectionPort`, `FinancePrincipalInvoicePort`); collected-cash reads only via `CollectedCashReadPort`; boundary tests guard `partner/billing/**` AND `partner/settlement/**`.
- Credit notes = additive balanced journal corrections + `finance_credit_corrections` ledger (no `finance_payments` rows, no `finance_invoices` schema change, no negative customer invoices); `recordRefund` remains full-refund-only of an existing settlement payment.
- SETTLEMENT invoices = principal-level (PLATFORM seller principal → PARTNER buyer principal) with the control-plane tenant as RLS/storage carrier only; conditional STANDARD vs SETTLEMENT constraints on `billing_invoices`.
- Settlement economics: negative eligible net preserved and carried (no clamp, no negative client invoice — charge floors at 0); finalized economics DB-level immutable (triggers); run model `CHECK (state <> 'COMPLETED' OR period_id IS NOT NULL)`; adjustments QUEUED→APPLIED with atomic claim + idempotency key; one Finance payment never economically allocated into two finalized periods.
- Snapshots: invoice UUID allocated first; snapshots INSERTed bound (`invoice_id` NOT NULL + FK shipped in W3 `V20260926_3`); `captureForInvoice(invoiceId, principalId, snapshotFor)`; no late-binding UPDATE.
- Issuance idempotency: `billing_invoices.issuance_idempotency_key` nullable + partial UNIQUE `AUTO:<subscriptionId>:<period>`; dual-scheduler race ⇒ exactly 1 invoice + 1 Finance mirror + 1 outbox fact.
- W1: immutable partial index `WHERE valid_until IS NULL` + companion expiry index (the `> now()` predicate form is rejected); `authorization_change_events` explicit two-branch USING+WITH CHECK policy (fail-closed; platform branch = NULL tenant + carrier GUC + partner NULL); audit contract test has no deferred-RED commit.
- W7: 6 finance tables ENABLE+FORCE from a NO-RLS baseline with the access-path matrix proven before merge; frozen 51-table CRM list (46 closures enumerated; the Rev B `crm_deals` sample name corrected — table does not exist); monotonic `authorization_version` backfill via dedicated sequence; canonical projection-backfill semantics; partner-isolation acceptance class created before CI lockstep; numeric G7-A soak; forward-only hardening rollback (permissive-RLS compensating scripts FORBIDDEN); all committed defaults OFF/legacy.
- The 31-test CI trio is extended only in W7 (now 7 classes incl. the formerly-phantom partner-isolation acceptance class) with the INLINE ci.yml maps updated from machine-derived surefire counts in the same commit (no `tests/ci/*.py` map exists; no web job in `ci.yml` — web gates in `post-merge-verification.yml`).
- PARTNER execution provider requires registration in two files (contract tests + execution dashboard) — both listed in W6 Task 9.

## D. Task-count parse + scan evidence (recalculated mechanically from the Rev C files)

Parsed object: `### Task N:` headings per wave file, parsed mechanically in this session (`rg -c "^### Task [0-9]+:"`), sequences verified contiguous 1..N per wave:

```text
WAVE_1_TASK_COUNT = 17
WAVE_2_TASK_COUNT = 14
WAVE_3_TASK_COUNT = 13
WAVE_4_TASK_COUNT = 21
WAVE_5_TASK_COUNT = 16
WAVE_6_TASK_COUNT = 10
WAVE_7_TASK_COUNT = 15
TOTAL_TASK_COUNT  = 106
```

(The Rev B parse of 104 is superseded: +1 W5 `CollectedCashReadPort` task, +1 W7 partner-isolation acceptance task; W4 task count unchanged — Task 4 and Task 18 were rewritten in place.)

Placeholder scan result: ZERO occurrences of any banned shorthand in the ten Rev C files (Unicode ellipsis; reused-command phrases; lettered matrix/partner count placeholders; generic count-deferral phrases; generic battery phrases; TBD markers; star-suffixed schematic column lists used as DDL). Remaining hits are negative-rule statements (this checklist, the W6/spec COALESCE prohibitions, the W1 index-predicate correction note), never schema or commands. All remaining commands are exact (cwd + Maven flags + fully-qualified class names).
