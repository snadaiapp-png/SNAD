# Self-Review & Coverage Matrix — Corrected Plans vs Design Spec Revision D (§1–§34)

**Reviewed artifacts:** spec + master plan + 7 wave subplans + this checklist as corrected on branch `review/unified-control-plane-revision-c-reconstructed` (**Revision D / R3 final correction — REGENERATED FROM SCRATCH after the R3 correction pass; Rev C statuses were NOT carried over mechanically; every row was re-evaluated against the Revision D documents**), against `docs/superpowers/specs/2026-09-23-unified-authorization-partner-billing-design.md` **Revision D**. Provenance (PROVENANCE ONLY, not the effective planning identity): Rev A merged @ `8d0d49c7bdd3ad3a886a23cffc1e735e61998712` (PR #1140); Rev B recovered byte-exact (`REVISION_B_DOCUMENT_RECOVERY=10/10_EXACT`, Rev B base commit `cb5ef42c7ece6bca5aa401356d4b9ae52219720c`); published reconstructed Revision C SHA `55046328da9cd390a79d9c981adf75c38dbed349`; Revision D / R3 final corrections written on top.
**Verdict rule (Revision D):** statuses are limited to `COVERED`, `PARTIAL`, `NOT_APPLICABLE_WITH_REASON`, `BLOCKED`. Nothing is pre-marked COVERED; each row names the plan, task, and test that will prove it; COVERED requires the corrected source text AND a named test/gate to actually support it. BLOCKED rows are listed EXPLICITLY (this review does NOT claim "BLOCKED rows: none" — two out-of-scope dependency blockers remain, see §A2).

## A. Spec section coverage (§ → requirement → plan → task → test → status)

| SPEC SECTION | REQUIREMENT | PLAN | TASK | TEST | STATUS |
|---|---|---|---|---|---|
| §1 | Purpose / scope statement | Master §0 | — | — | NOT_APPLICABLE_WITH_REASON=scope narrative; no executable requirement |
| §2 | Extend existing foundations; backward compat | Master §0 table | all waves | W1 Task 8 `EvaluateFacadeCompatibilityTest` | COVERED |
| §3 | Hierarchical multi-principal platform + boundaries | W2 | Task 1, Task 12 | `PartnerPrincipalRlsPostgresTest`; portal IT | COVERED |
| §4.1 | Five-stage runtime algorithm (A–E); protected safety NOT a precedence layer; no `PROTECTED_SAFETY` source | W1 | Task 7, Task 8, Task 11 | `UnifiedDecisionPipelineTest`; `DenyDominancePostgresTest`; reflection scan on `DecisionSource` | COVERED |
| §4.2 | Fail-closed list | W1 | Task 7 | `UnifiedDecisionPipelineTest` cases a–c,f,i | COVERED |
| §5 | Protected roles = PLATFORM_OWNER, PLATFORM_ADMIN, AGENT_SUPER_ADMIN, TENANT_ADMIN; AGENT_CUSTOM_ADMIN NOT protected | W1+W2+W7 | W1 Task 3/14, W2 Task 11, W7 Task 3 | `ProtectedRoleRegistrySchemaPostgresTest`; `ProtectedRoleGrantGuardTest`; `ProtectedSeedFourRolesPostgresTest` | COVERED |
| §5.1 | Break-glass rejections + simulation + break-glass evaluated normally (DENY still wins) | W1+W7 | W1 Task 15, W7 Task 5 | `BreakGlassAccessServiceTest`; `BreakGlassInvariantPostgresAcceptanceTest` | COVERED |
| §6 | Registry metadata columns | W1 | Task 4 | `CapabilityMetadataMigrationContractTest` | COVERED |
| §6.1 | ONE canonical Capability Contract Table; no duplicate namespace; full-chain VIEW/MANAGE/FINALIZE + `SUBSCRIPTION.MANAGE` end-to-end (Revision D: present in spec §6.1 table, W2 Task 5 seed list of 12 codes, W2 Task 10 allowlist + positive test, W2 Task 12 consumption, W4 continuation tests + UI gating) | W2+W3+W4+W5 | W2 Tasks 5/10/12, W3 Task 6, W4 Tasks 8/9/11/20, W5 Task 14 | `PartnerCapabilitySeedContractTest` (asserts `SUBSCRIPTION.MANAGE`); `CommercialCapabilitySeedContractTest`; `BillingCapabilitySeedContractTest`; `PartnerDelegationGateTest` (positive `SUBSCRIPTION.MANAGE` delegation case); `SettlementControllersIT`; `BillingForeignKeyAuditPostgresTest` | COVERED |
| §7 | RBAC + ABAC + ReBAC-lite; explicit relationships; no fuzzy matching | W1 | Task 7, Task 12 | pipeline + `EffectivePermissionProjectionServiceTest` + `HrScopedRelationshipResolver` | COVERED |
| §8 | User/subject vs employee; service accounts as subjects | W1 (existing `users` table = subjects) | documented mapping (master §0) | no schema change required — existing identity tests | NOT_APPLICABLE_WITH_REASON=design reuses the existing users/employees model; no new executable behavior |
| §9 | Overrides + Direct DENY v1 DB invariant + precedence correction | W1 | Task 1, Task 9, Task 11 | `UserPermissionOverrideRlsPostgresTest` (23514 on scoped DENY); `UserPermissionOverrideServiceTest`; `DenyDominancePostgresTest` | COVERED |
| §10 | Dynamic scope family + capability-declared scopes | W1 | Task 1, Task 9 | scope CHECK + `supports_scope` validation matrix | COVERED |
| §11 | Explainable decisions + cache model | W1 | Task 7, Task 13 | `AuthorizationDecision` record; `AuthorizationInvalidationListenerTest` | COVERED |
| §12 | Entitlements vs permissions; recalc vs resync | W1 (+ existing entitlement gate) | Task 10 | `UserOverrideControllerIT` resync 403/200 | COVERED |
| §13 | Delegation over canonical capabilities; scope from authenticated context; `SUBSCRIPTION.MANAGE` in the delegation vocabulary (Revision D) | W2 | Task 10, Task 12 | `PartnerDelegationGateTest`; `PartnerPortalControllerIT` | COVERED |
| §13.1 | One ACTIVE partner membership per user; ALL FOUR membership mutations (create-ACTIVE, suspend, role downgrade, partner transfer) invalidate session state immediately in the SAME transaction (mutate + `session_version` bump + `SessionVersionCache.invalidate` + partner-membership/JWT-claim cache eviction); old JWT denied on the very next request; new JWT reflects the new state immediately with no sleep/TTL wait (Revision D) | W2 | Task 1, Task 6 (G5), Task 7 (four-mutation invariant) | `PartnerPrincipalRlsPostgresTest` (23505 race); `PartnerMembershipConcurrencyPostgresTest`; `PartnerSessionInvalidationTest` | COVERED |
| §14 | Business identity fields + 3 mounts | W3 | Task 1, Task 8, Task 11 | `BusinessIdentityRlsPostgresTest`; `BusinessIdentityServiceTest`; controller ITs | COVERED |
| §14.1 | Reusable Commercial & Tax Information screen | W3 | Task 12 | `commercial-api.test.ts` + form component tests | COVERED |
| §14.2 | White-label readiness (branding modeled separately) | W3 | Task 1/9 schema | `brand_profiles` separate table asserted in `LogoUploadServiceTest` | COVERED |
| §14.3 | platform_files: real schema, no `kind`, ownership CHECK, ONE fail-closed principal policy with THREE DISJOINT branches (TENANT branch requires `partner_id IS NULL`; PARTNER branch requires carrier + `partner_id = app.partner_id`; PLATFORM branch requires carrier + `partner_id IS NULL` + `app.partner_id IS NULL`), explicit partner-carrier negative, `pg_policy` verification, registration-of-already-stored-object scope with NO magic-byte/content-sniffing claim (Revision D) | W3 | Task 2, Task 3, Task 9 | `PlatformFilesForceRlsPostgresTest` (incl. case (e2) carrier-cannot-read-platform-rows + `pg_policy` assertions); `WorkflowAttachmentExternalFoundationTest`; `LogoUploadServiceTest` (declared-MIME allowlist only) | COVERED for the registration scope — the byte-upload capability itself is BLOCKED out-of-scope (see §A2, BLK-1) |
| §15 | Immutable billing identity snapshots; allocate-UUID-first capture order | W3+W4 | W3 Tasks 4/5/10, W4 Tasks 4/14 | `SnapshotImmutabilityPostgresTest`; `SnapshotInvoiceBindingPostgresTest`; issuance chain test | COVERED |
| §16 | DIRECT/RESELLER/COMMISSION/HYBRID | W2+W4 | W2 Task 1, W4 Task 1 | CHECK-constraint assertions in schema tests | COVERED |
| §17 | Partner→tenant invoices MANUAL/AUTOMATIC | W4 | Task 14, Task 17 | `PartnerInvoiceIssuancePostgresTest`; `AutomaticBillingPostgresTest` | COVERED |
| §17.2 | Auto-invoicing carrier = subscription; five-condition scheduler; auditable confirmation; DB-level dual-scheduler idempotency | W4 | Task 2 (`issuance_idempotency_key`), Task 5, Task 6, Task 17 | `LifecycleGateStatusesPostgresTest` (negative column assertion on billing_invoices); `AutomaticBillingPostgresTest` (5 negatives + two-thread single-invoice proof) | COVERED |
| §18.1 | Settlement basis B: net collected; no double subtraction; NEGATIVE RESIDUAL PRESERVED AND CARRIED, never clamped, never a negative customer invoice | W5 | Task 4, Task 1 (period CHECK change) | `EligibleNetCalculatorTest` matrix incl. negative-carryover edges | COVERED |
| §18.2 | Fee percent unambiguous (agreement version field) | W4+W5 | W4 Task 1, W5 Task 5 | CHECK bounds; per-item math | COVERED |
| §18.3 | Version immutability + invoice pinning + temporal exclusion | W4 | Task 1, Task 16 | `AgreementTemporalIntegrityPostgresTest` (23P01 + same-start race + open-ended block); `AgreementVersionPinningPostgresTest` | COVERED |
| §18.4 | Settlement lifecycle replay-safe + reconcilable; COMPLETED run always binds its period | W5 | Task 5, Task 6, Task 12 | `PartnerSettlementCalculatePostgresTest`; `SettlementReplayReplacePostgresTest`; `SettlementReconciliationPostgresTest`; Task 1 CHECK assertions | COVERED |
| §18.5 | Period state machine: full rebuild; same key no-op; new key replace; finalize immutable at DB level; concurrency | W5 | Task 1 (immutability triggers), Task 6, Task 10 | `SettlementReplayReplacePostgresTest`; `SettlementFinalizeConcurrencyPostgresTest`; trigger assertions in `SettlementSchemaFkPostgresTest` | COVERED |
| §18.6 | Late adjustments = QUEUED→APPLIED with atomic claim + idempotency key; finalized history untouched | W5 | Task 1 (adjustment model), Task 11 | `LateAdjustmentPostgresTest` (claim concurrency + replay no-op) | COVERED |
| §18.7 | NO persisted period fee; derived weighted display only | W5+W6 | W5 Task 1 (negative column assertion), W6 Task 4 (no column), W5 Task 15 (UI label) | `SettlementSchemaFkPostgresTest`; `ProjectionSchemaExactPostgresTest` | COVERED |
| §19 | Finance authoritative; no second ledger; boundary contract via Finance-owned ports incl. `CollectedCashReadPort`; additive correction authority `FinanceCorrectionPort` with the EXACT Revision D canonical journal semantics (`preTaxCorrectionMinor = grossCorrectionMinor − taxMinor`; DEBIT contra-revenue by pre-tax; IF tax>0 DEBIT tax-payable relief; CREDIT AR by gross; debits == credits ⇒ 3 non-zero lines when tax>0, 2 when tax=0; `FinanceCorrectionAccountingPostgresTest` asserts roles/signed amounts/zero-sum, never a hard-coded count); `remainingCreditableGross` creditable limit (OPEN or PAID invoice; NEVER the AR open balance); additive idempotent void reversal; principal-invoice port `FinancePrincipalInvoicePort.ensurePrincipalInvoice` for SETTLEMENT invoices — never `SubscriptionFinancePort` (Revision D) | W4/W5 | W4 Tasks 14/15/18, W5 Tasks 8/10 | `markInvoicePaid` 409; `R0C13ArchitectureBoundaryTest` + `FinanceReadBoundaryTest`; `FinanceCorrectionAccountingPostgresTest`; principal-invoice assertions in `SettlementApprovalPostgresTest` | COVERED |
| §20 | Logical data model (tables) | W1–W6 migrations | per-wave schema tasks | schema/RLS tests per wave | COVERED |
| §21 | Notification events list + mandatory owner in-product delivery | W6 | Task 2 | `MandatoryOwnerNotificationPostgresTest` | COVERED |
| §21.1 | Denormalized scope + consistency CHECKs + explicit RLS + isolation negatives + ack ownership + audience-source separation + NO COALESCE bypass | W6 | Task 1, Task 3 | `NotificationSchemaRlsPostgresTest`; `NotificationIsolationPostgresTest` (incl. forged-source negative) | COVERED |
| §22 | Three dashboard scopes + governed projections | W6 | Task 4, Task 6, Task 8 | `ProjectionSchemaExactPostgresTest`; `PartnerProjectionPostgresTest`; controller ITs | COVERED |
| §22.4 | Exact typed projection schema; no schematic placeholders; typed apply-log | W6 | Task 4, Task 5 | `ProjectionSchemaExactPostgresTest` (name-by-name); `ProjectionEventTypingPostgresTest` | COVERED |
| §22.5 | GLOBAL = DIRECT + Σ(PARTNERS); fixture ≥2 partners + ≥1 direct tenant | W6 | Task 7 | `DashboardReconciliationPostgresTest` | COVERED |
| §23 | Invoice dashboards (owner + partner views) | W6 | Task 8, Task 9 | `DashboardControllerIT`; FE gating tests | COVERED |
| §24 | Executive/partner/tenant IA | W2/W3/W6 | W2 Task 13, W3 Task 12, W6 Task 9 | FE gating tests + visual-matrix baselines | PARTIAL=component/gating tests only; no cross-browser E2E harness exists in the repo (visual-matrix baselines added in W6 Task 9; execution evidence pending) |
| §25 | Authorization administration UI + user detail panels | W1 | Task 16 | `access-api.test.ts`; `authorization-page-gating.test.tsx` | COVERED |
| §26 | Event-driven invalidation + projections; atomic outbox/audit; canonical projection-backfill semantics (deterministic rebuild, idempotent, version-stamped, count-invariant) | W1+W6+W7 | W1 Task 13, W6 Task 6, W7 Task 3 | listener tests; fan-out same-transaction assertions; `ProjectionBackfillPostgresTest` (rebuild-twice checksum + monotonic versions) | COVERED |
| §27 | Audit record fields; DENY/high-risk auditable; no commit-RED-then-later-GREEN in audit TDD | W1 (+ existing PlatformAuditWriter) | Task 6 (migration only), Task 10 (contract test RED→GREEN in one task) | `AccessAdminAuditContractTest` (created in W1 Task 10) | COVERED |
| §28 | API categories fixed (routes are plan details) | W1/W2/W4/W5/W6 controllers | per-wave controller ITs | MockMvc ITs | COVERED |
| §29 | Migration strategy phases 1–9 | Master §1 mapping | per-wave | per-wave | COVERED |
| §29.1 | Progressive cutover G7-A..G7-G; per-stage gates; no big bang; NUMERIC G7-A soak (≥7 days AND ≥50k decisions, divergence 0, p99 ≤10 %); same-SHA contract; committed defaults OFF/legacy with DEPLOYMENT-ENV-ONLY stage activation (Revision D: G7-B..G7-G stage tasks commit no config flip, evidence records deployment env values + source SHA, rollback changes env values only); forward-only hardening rollback; per-stage observability implementation+query+evidence | W7 | Tasks 8–14 + Global Constraints 3/4 | stage smoke ITs + per-stage records + soak rows in evidence logs | COVERED |
| §30 | 10 release-blocking security invariants | W7 | Task 4 | `SecurityInvariantsPostgresAcceptanceTest` (class explicitly created by Task 4) | COVERED |
| §31.1 | Authorization matrix (subjects × dimensions) | W7 | Task 5 | `AuthorizationMatrixPostgresAcceptanceTest` (class explicitly created by Task 5) | COVERED |
| §31.2 | Break-glass test list (7 rejections) | W7 | Task 5 | `BreakGlassInvariantPostgresAcceptanceTest` | COVERED |
| §31.3 | Billing/settlement mandatory tests incl. the revision additions (issuance idempotency, boundary test, journal-correction semantics with mathematically correct 2-or-3-line model, `remainingCreditableGross` validation, negative-carryover matrix, single-allocation concurrency, congruence chain, run-model CHECK, adjustment claim atomicity, principal-invoice port) | W4+W5 | W4 Tasks 2/14–18, W5 Tasks 4–13 | named per task (`FinanceCorrectionAccountingPostgresTest`, `FinanceReadBoundaryTest`, `AutomaticBillingPostgresTest` two-thread proof, `LateAdjustmentPostgresTest` claim concurrency, `SettlementApprovalPostgresTest` congruence chain) | COVERED |
| §31.4 | Dashboard reconciliation + direct-API isolation | W6 | Task 7, Task 8 | reconciliation + isolation tests | COVERED |
| §31.5 | PostgreSQL Direct mandatory | all waves | every PG test | local + pg-acceptance profiles with exact commands | COVERED |
| §31.6 | Correction-mandated negative/concurrency tests (19 lines) | W1/W2/W3/W4/W5/W6 | W1 Task 11, W2 Task 7, W3 Tasks 1/2, W4 Task 1, W5 Tasks 6/10/11, W6 Tasks 3/7 | each line mapped to a named test class | COVERED |
| §32 | Release acceptance criteria list incl. the protected release chain with REQUIRED-CHECKS DISCOVERY gate (release-claim-only blocker, Revision D) and the authenticated desktop/mobile + RTL/accessibility final gate | W7 | Task 15 | verification runbook §4 steps 7–8 | PARTIAL for the RTL/accessibility/mobile line (depends on visual-matrix baselines added W6 Task 9; execution evidence can only exist after implementation) and BLOCKED for the required-checks discovery line at PLANNING time (see §A2, BLK-2) |
| §33 | Non-goals (no ZATCA/live-payments claims) | W4/W5/W7 | flags unchanged (`R0C13_PROVIDER_MODE`) | documented invariant | NOT_APPLICABLE_WITH_REASON=negative space; compliance certification is out of scope by design |
| §34 | Final architecture decision | Master §0 + waves | — | — | NOT_APPLICABLE_WITH_REASON=architecture statement; no executable requirement |

## A2. Explicit BLOCKED rows (Revision D — no "zero blockers" claim)

| ID | ITEM | STATUS | EVIDENCE | RESOLUTION PATH |
|---|---|---|---|---|
| BLK-1 | `REAL_BYTE_STORAGE_ADAPTER` — real byte upload (browser → object storage) for logos/attachments | **BLOCKED — out-of-scope dependency** | No byte-storage adapter exists: `PlatformFileReferenceService` is registration-only (caller-supplied `storage_reference`; javadoc: "no object storage / file service exists in the deployment"); zero S3/MinIO/GCS/Azure deps in `apps/sanad-platform/pom.xml` | Revision D scopes W3 to registration of an already-stored object + `StorageAdapter` port contract (`W3_STORAGE_ADAPTER` resolved BY SCOPE LIMIT); `LogoUploadService.registerStored(principalId, storageReference, mimeType, sizeBytes, checksumSha256)` receives NO bytes and makes NO magic-byte/content-sniffing claim — validation covers only declared MIME allowlist (`image/png`/`image/jpeg`), `sizeBytes > 0 && ≤ 2_097_152`, 64-hex checksum format, principal congruence, and the atomic registration transaction; the byte-upload capability (and real magic-byte/content verification on the read path) is an explicit future dependency requiring a storage-infrastructure decision. Does NOT block W3 execution as scoped; DOES block any claim of end-to-end multipart upload or content validation |
| BLK-2 | `RELEASE_REQUIRED_CHECKS_DISCOVERY` — current branch-protection required-check set | **BLOCKED — release-gate dependency** | GitHub API `GET /repos/snadaiapp-png/SNAD/branches/main/protection` ⇒ HTTP 403 (unauthenticated; `gh` CLI absent); the required-check set is NOT in git and CANNOT be read at planning time | Revision D keeps this an explicit release-chain gate: discovery with authenticated token evidence (`snad-evidence/required-checks.json`) BEFORE any release claim; candidate job names recorded as candidates only; failure to discover blocks a RELEASE CLAIM only — never docs-only planning or implementation of earlier waves. Does NOT block planning/implementation; DOES block any release claim until discovery evidence exists |

`FINANCE_CREDIT_NOTE_MODEL` is NOT listed as blocked: fresh evidence confirmed no generic partial credit-note primitive exists (`recordRefund` is full-refund-only; adapter throws on partial), and the revision resolves it by PLANNING the Finance-owned additive correction model with the exact canonical accounting semantics as W4 Task 18 (`FinanceCorrectionPort` + `finance_credit_corrections` + balanced 2-or-3-line journal entries + `remainingCreditableGross` validation) — the resolution path the review directive prescribes. `W3_STORAGE_ADAPTER` likewise is resolved BY SCOPE LIMIT for the wave, with the residual byte-upload dependency carried honestly as BLK-1.

## B. Quality gates applied to the corrected files (Revision D)

1. **Placeholder/shorthand scan** — the ten corrected files were scanned for: command ellipses, cross-row command references, lettered count placeholders, generic unwritten-count phrases, generic "battery by name only" references, TBD markers, and schematic wildcard DDL; additionally for the FORBIDDEN GUC bypass pattern `COALESCE(current_setting('app.current_user_id', true)` — the only remaining occurrences are the prohibition statements themselves (W6 header/Task 1, spec §21.1); no policy or command uses it. Scan executed mechanically; method and result in §D.
2. **Revision D mechanical final scans (order §9)** — executed over the ten rewritten files: stale `Spec:** Revision B|Spec:** Revision C|Rev B is binding|Rev C is binding` = 0 live hits (remaining older-revision strings are explicitly labelled provenance records); `SUBSCRIPTION.MANAGE` present in W2 + spec; no `magic-byte|%PNG|FF D8 FF` metadata-validation claims in W3/spec (only prohibition/honesty statements); no `exactly two.*journal|exactly 2.*balanced` wording; no `SETTLEMENT.*SubscriptionFinancePort` mirror claim; no `evidence): gate run`-style tracked evidence commits; no `UPDATE users SET authorization_version = nextval`; no committed `stage default true` instructions; no broken `rate(...])` PromQL forms.
3. **Interface consistency** — table names, event names, scope names, endpoint prefixes, capability codes (`SUBSCRIPTION.MANAGE` continuation authority; `SETTLEMENT.VIEW/MANAGE/FINALIZE` chain), DTO names, port contracts (`FinanceCorrectionPort`, `FinancePrincipalInvoicePort`, `CollectedCashReadPort`) cross-checked within and across waves and against spec §6.1.
4. **Requirement coverage** — table §A; no approved requirement silently absent; architecture-only rows carry explicit `NOT_APPLICABLE_WITH_REASON`; dependency blockers carried explicitly in §A2.
5. **Two-class TDD structure (Revision D)** — implementation tasks carry Files/Interfaces and the RED → minimal implementation → GREEN → regression → commit chain; the seven verification/evidence tasks (W1 T17, W2 T14, W3 T13, W4 T21, W5 T16, W6 T10, W7 T15) are written as PRECONDITION → VERIFY → EVIDENCE with untracked artifacts and end with `NO TRACKED COMMIT — evidence external/untracked; report FINAL_WAVE_SHA/STAGE_SHA`; no task fabricates a RED test and no task plans a tracked evidence commit after a same-SHA gate; W7 Tasks 4/5/6 explicitly create their acceptance classes (no `none` while a class is absent).

## C. Consistency decisions recorded (Revision D)

- `DecisionSource` = `EXPLICIT_DENY, EXPLICIT_ALLOW, ROLE_GRANT, RELATIONSHIP_POLICY, DELEGATED_GRANT, DEFAULT_DENY` — no `PROTECTED_SAFETY` (spec §9).
- Protected registry = exactly 4 codes in W1 migration, W2 grant guard, and W7 seeding; `AGENT_CUSTOM_ADMIN` is a normal hierarchy role (spec §5).
- GUC contract: three GUCs (`app.tenant_id`, `app.partner_id`, `app.current_user_id`) — all sourced from verified server-side authentication; missing ⇒ fail-closed; W2 introduces `app.current_user_id`, W6 consumes it; background jobs have NO implicit bypass (W7 access-path matrix row 7); the `COALESCE(current_setting('app.current_user_id', true), …)` bypass pattern is FORBIDDEN everywhere.
- `platform_files` RLS = ONE policy `principal_isolation` (ENABLE+FORCE) with three disjoint branches exactly per Revision D: TENANT (`tenant_id = app.tenant_id` AND `tenant_id != CONTROL_PLANE_TENANT` AND `partner_id IS NULL`), PARTNER (carrier + `partner_id IS NOT NULL` + `partner_id = app.partner_id`), PLATFORM (carrier + `partner_id IS NULL` + carrier GUC + `app.partner_id IS NULL`); the migration drops the EXACT existing policy name `platform_files_tenant_isolation` first; the partner-carrier-cannot-see-platform-rows negative is explicitly tested.
- Logo registration honesty: `registerStored` receives no bytes — declared-MIME allowlist, size cap, 64-hex checksum format, principal congruence, atomic transaction ONLY; no magic-byte/content-sniffing claim anywhere; `REAL_BYTE_STORAGE_ADAPTER` stays explicitly BLOCKED out-of-scope (BLK-1).
- `SUBSCRIPTION.MANAGE` end-to-end: seeded in W2 `V20260925_5` (12-code seed list), in the W2 Task 10 allowlist (exact canonical 12-code set, no `PARTNER.SUBSCRIPTION.*` namespace), positively tested in `PartnerDelegationGateTest`, consumed by W2 Task 12 and W4 continuation surfaces (`ContinuationConfirmationService` + UI gate `SUBSCRIPTION.MANAGE`).
- W2 Task 7 immediate invalidation: for EACH of create-ACTIVE / suspend / role downgrade / partner transfer, in the SAME transaction: mutate membership; bump `users.session_version`; `SessionVersionCache.invalidate(tenantId, userId)`; evict the partner-membership/JWT-claim cache entry. Test proves old JWT 401 on the very next request for all four and the new JWT reflects the new state immediately with no sleep.
- Finance boundary: writes only via Finance-owned ports (`SubscriptionFinancePort` tenant/subscription invoices, `FinanceCorrectionPort` corrections, `FinancePrincipalInvoicePort` SETTLEMENT principal invoices); collected-cash reads only via `CollectedCashReadPort`; boundary tests guard `partner/billing/**` AND `partner/settlement/**`; SETTLEMENT invoices NEVER ride `SubscriptionFinancePort.ensureInvoice(tenantId, …)` and no fabricated tenant/subscription UUID represents the partner principal.
- Credit notes = additive balanced journal corrections with the exact canonical line model (3 non-zero lines when `taxMinor > 0`, 2 when `taxMinor = 0`; debits == credits; roles/signed amounts/zero-sum asserted — no hard-coded contradictory count) + `finance_credit_corrections` ledger (no `finance_payments` rows, no `finance_invoices` schema change, no negative customer invoices); creditability via `remainingCreditableGross` (OPEN or PAID creditable — never the AR open balance); void = additive reversing correction linked idempotently; `recordRefund` remains full-refund-only of an existing settlement payment.
- Settlement economics: negative eligible net preserved and carried (no clamp, no negative client invoice — charge floors at 0); finalized economics DB-level immutable (triggers); run model `CHECK (state <> 'COMPLETED' OR period_id IS NOT NULL)`; adjustments QUEUED→APPLIED with atomic claim + idempotency key; one Finance payment never economically allocated into two finalized periods.
- Snapshots: invoice UUID allocated first; snapshots INSERTed bound (`invoice_id` NOT NULL + FK shipped in W3 `V20260926_3`); `captureForInvoice(invoiceId, principalId, snapshotFor)`; no late-binding UPDATE.
- Issuance idempotency: `billing_invoices.issuance_idempotency_key` nullable + partial UNIQUE `AUTO:<subscriptionId>:<period>`; dual-scheduler race ⇒ exactly 1 invoice + 1 Finance mirror + 1 outbox fact.
- W1: immutable partial index `WHERE valid_until IS NULL` + companion expiry index; `authorization_change_events` explicit two-branch USING+WITH CHECK policy (fail-closed); audit contract test has no deferred-RED commit.
- W7: 6 finance tables ENABLE+FORCE from a NO-RLS baseline with the access-path matrix proven before merge; frozen 51-table CRM list; deterministic `authorization_version` ranked-mapping backfill (`ROW_NUMBER() OVER (ORDER BY created_at, id)` assigned values, never `nextval()` inside an unordered UPDATE; `uac_authorization_version_seq` positioned with `setval(..., is_called)`; rerun idempotent; sequence-next > every assigned version); acceptance classes explicitly created by their own tasks; monotonic sequence for future bumps; canonical projection-backfill semantics; partner-isolation acceptance class created before CI lockstep; numeric G7-A soak with concrete range vectors (`[5m]`, `[24h]`, `[168h]` — no placeholder windows); forward-only hardening rollback (permissive-RLS compensating scripts FORBIDDEN); all committed defaults OFF/legacy with stage activation DEPLOYMENT-ENV ONLY.
- The 31-test CI trio is extended only in W7 (7 classes incl. the formerly-phantom partner-isolation acceptance class) with the INLINE ci.yml maps updated from machine-derived surefire counts in the same commit.
- PARTNER execution provider requires registration in two files (contract tests + execution dashboard) — both listed in W6 Task 9.

## D. Task-count parse + scan evidence (recalculated mechanically from the Revision D files)

Parsed object: `### Task N:` headings per wave file, parsed mechanically (`rg -c "^### Task [0-9]+:"`), sequences verified contiguous 1..N per wave:

```text
WAVE_1_TASK_COUNT = 17
WAVE_2_TASK_COUNT = 14
WAVE_3_TASK_COUNT = 13
WAVE_4_TASK_COUNT = 21
WAVE_5_TASK_COUNT = 16
WAVE_6_TASK_COUNT = 10
WAVE_7_TASK_COUNT = 15
TOTAL_TASK_COUNT  = 106
```

(The Rev B parse of 104 is superseded: +1 W5 `CollectedCashReadPort` task, +1 W7 partner-isolation acceptance task; W4 task count unchanged — Task 4 and Task 18 were rewritten in place. The Revision D correction changes NO task counts.)

Placeholder scan result: ZERO occurrences of any banned shorthand in the ten Revision D files. Remaining hits are negative-rule statements (this checklist, the W6/spec COALESCE prohibitions, the W1 index-predicate correction note, the D-21/D-30/D-50/D-52/D-53 prohibition statements), never schema or commands. All remaining commands are exact (cwd + Maven flags + fully-qualified class names).
