# CRM-003R Pull Request Gate

The corrective pull request must remain unmerged until the exact current head
has no failed, queued, or in-progress required checks and the dedicated
PostgreSQL acceptance reports zero skipped tests.
