/** System Health route registry. */
export const systemHealthRoutes = {
  root: "/system-health",
  dashboard: "/system-health",
  systems: "/system-health",
  actions: "/system-health/actions",
} as const;
