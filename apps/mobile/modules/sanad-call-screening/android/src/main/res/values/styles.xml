<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal translucent card theme for SanadCallerIdActivity (G8-05 §36). -->
    <style name="Theme.SanadCallerIdCard" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
