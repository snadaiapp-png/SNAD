# Production release marker directory

Files in this directory are inert release-control artifacts. They may trigger backend image publication through the repository path filter, but they do not change application runtime behavior.
