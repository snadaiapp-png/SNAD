package com.sanad.platform.hr.compensation.domain;

/** Canonical compensation component types (WS6 Task 3, structural only). */
public enum CompensationComponentType {
    BASE_SALARY,
    ALLOWANCE,
    BENEFIT,
    VARIABLE_TARGET,
    OTHER
}
