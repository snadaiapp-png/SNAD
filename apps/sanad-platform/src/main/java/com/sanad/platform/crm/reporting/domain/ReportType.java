package com.sanad.platform.crm.reporting.domain;

/**
 * Enumeration of supported CRM report types.
 */
public enum ReportType {
    LEAD_PIPELINE,
    OPPORTUNITY_PIPELINE,
    ACTIVITY_SUMMARY,
    EMAIL_ENGAGEMENT,
    CONVERSION_FUNNEL,
    SALES_FORECAST
}
